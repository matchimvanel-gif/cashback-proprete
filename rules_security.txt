Firestore.rules initiales (à compléter) :

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Utilisateurs
    match /utilisateurs/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Dépôts/Coupons (citoyen)
    match /depots/{depotId} {
      allow read, create: if request.auth != null && resource.data.uid_citoyen == request.auth.uid;
      allow update: if request.auth != null;
    }
    
    // À compléter avec Hyzakam, partenaires...
  }
}

