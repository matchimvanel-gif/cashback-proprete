**CLOUD FUNCTIONS - CASHBACK PROPRETÉ**

**addDepot** (functions/src/addDepot.js):
- **Fonction**: Enregistre dépôt responsable → incrémente points citoyen + fill bac.
- **Extrait**:
```js
exports.addDepot = functions.https.onCall(async (data, context) => {
  const { id_citoyen, id_agent, categorie, poids, point, bonus_tri } = data;
  const batch = admin.firestore().batch();
  // Depot + user point + bac fill
  await batch.commit();
  return { success: true, depotId, points };
});
```
- **Fonctionnement**: Callable depuis app (responsable confirm). Auth check, batch writes (atomic). Logs errors.

**Autres prévues** (pseudo-code):
1. **updateMontantContrat** (cron mensuel):
```js
exports.updateMontantContrat = functions.pubsub.schedule('0 0 1 * *').onRun(() => {
  // Update etablissements based cote + depots volume
});
```

**Deploy**: `firebase deploy --only functions:addDepot`

